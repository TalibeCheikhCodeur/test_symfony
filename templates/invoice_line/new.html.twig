{% extends 'base.html.twig' %}

{% block title %}New Invoice Line{% endblock %}

{% block body %}
    <h1>Create new Invoice Line</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">Save</button>
    {{ form_end(form) }}

    <a href="{{ path('invoice_line_index') }}" class="btn btn-secondary">Back to list</a>
{% endblock %}
